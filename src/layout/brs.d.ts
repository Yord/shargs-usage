import {Style} from '../style'

export const brs:                     (length?: number) => (style?: Style) => string
export const brsFrom: (id: string) => (length?: number) => (style?: Style) => string